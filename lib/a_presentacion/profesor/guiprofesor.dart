import 'package:flutter/material.dart';

class ProfesorScreen extends StatelessWidget {
  const ProfesorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Profesor'),),
      body: Placeholder(),
    );
  }
}