import 'package:flutter/material.dart';

class MetodologoScreen extends StatelessWidget {
  const MetodologoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Metodologo'),),
      body: Placeholder(),
    );
  }
}